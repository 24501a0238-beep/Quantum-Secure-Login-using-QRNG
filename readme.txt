Tested with Python 3.13.7
open cmd and install following packages
pip install flask
pip install qiskit
pip install qiskit-aer
pip install matplotlib
pip install numpy

open CMD and run
cd <directory path>
python app.py

check CMD for mobile otp for testing purposes